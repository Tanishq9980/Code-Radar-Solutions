#include <stdio.h>
int main()
{
    int N;
    int flag=0;
    scanf("%d",&N);
    int a[N];
    for(int i=0;i<N;i++){
        scanf("%d",&a[i]);
    }
if(a[0]>a[1]; return arr[0];)
else return -1;
    for(int i=1;i<N-1;i++)
    {
        if(a[i-1]<a[i] && a[i]>a[i+1])
        flag=a[i];
    }
    printf("%d",flag);

    if(arr[N-1]>arr[N-2]; return arr[N-1];)
    else return -1;
   return -1;
}