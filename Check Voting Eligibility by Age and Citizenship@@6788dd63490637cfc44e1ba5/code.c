#include <stdio.h>


int main() {
    int x,y;
    scanf("%d %d",&x,&y);
    if(x>18)
       if(1)
       {
        printf("Eligible");
        return 1;
       }
    printf("Not Eligible");
    return 0;
}